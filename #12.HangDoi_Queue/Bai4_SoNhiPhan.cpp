// Cho số tự nhiên n. Hãy in ra tất cả các số nhị phân từ 1 đến n. Chú ý không in thừa các bit 0 ở đầu.

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<string> res;
    queue<int> Q;
    Q.push(1);
    while (1) {
        int top = Q.front();

    }
    return 0;
}